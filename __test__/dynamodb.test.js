const { mockDdb } = require('../dynamo-mock');

it('should insert payment into table', async () => {
  // await mockDdb
  //   .put({ TableName: 'reviews', Item: { reviewId: '1', likes: '1000' } })
  //   .promise();
  // const { Item } = await mockDdb
  //   .get({ TableName: 'reviews', Key: { reviewId: '1', amount: '1000' } })
  //   .promise();
  // expect(Item).toEqual({
  //   reviewId: '1',
  //   likes: '1000',
  // });
});
